{
  "name": "@sdn/xtce",
  "version": "1.0.0",
  "description": "XTCE (XML Telemetry/Command Exchange) to JSON Schema converter with FlatBuffer annotations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sdn-xtce": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "xtce",
    "ccsds",
    "telemetry",
    "spacecraft",
    "json-schema",
    "flatbuffers",
    "space-data-network"
  ],
  "author": "Space Data Network",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "fast-xml-parser": "^4.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
